SET(CMAKE_INCLUDE_CURRENT_DIR ON)

INCLUDE_DIRECTORIES(
    ${CMAKE_CURRENT_BINARY_DIR}
)

IF(BUILD_SHARED_LIBS)
	ADD_DEFINITIONS(-DFEI_SHARED_LIBS)
ENDIF(BUILD_SHARED_LIBS)

ADD_SUBDIRECTORY(report)

ADD_SUBDIRECTORY(locale)
